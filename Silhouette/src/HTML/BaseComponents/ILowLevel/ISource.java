package HTML.BaseComponents.ILowLevel;

// <source> tag should be easily auto-generated
// <picture>, <video> and <audio> uses the <source> tag
public interface ISource {
    IBuilder addSource(String src, String type);
}
